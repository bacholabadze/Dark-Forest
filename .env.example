VITE_HELIUS_KEY=
VITE_SOLANA_SECRET=
VITE_DEVNET_RPC=https://api.devnet.solana.com
# Optional — Claude API key for the live AI patrol-log narrator.
# Without it the log uses the scripted corpus and says so.
VITE_AI_KEY=
